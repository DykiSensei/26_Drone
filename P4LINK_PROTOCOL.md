# p4link — S3↔P4 串口通信协议 v1

> 面向 **P4 侧开发者**的完整规格：拿着本文档 + 一份 `components/communication/p4link_protocol.h`
> 拷贝即可独立开发，无需阅读飞控代码。
>
> **状态（2026-07-05）**：协议 v1 定稿，**S3 侧驱动已实现**（`communication/p4link.c`：
> UART2 收发 + 解析状态机 + 50Hz STATE 广播 + 全部安全门，无对端时静默待机）。
> ⚠️ P4 硬件未就绪，尚未联调实测。头文件中的结构体尺寸（18B/12B）与 CRC 实现
> （校验值 0x29B1）已在 S3 侧自测通过。P4 上线后 S3 侧无需任何改动即可对接。

---

## 1. 系统角色（先读这个）

```
┌──────────────┐  MSG_STATE 50Hz（姿态/高度/模式/爪角）  ┌──────────────┐
│  ESP32-S3    │ ───────────────────────────────────────▶ │  ESP32-P4    │
│  飞控        │                                          │  视觉        │
│  (本仓库)    │ ◀─────────────────────────────────────── │  (相机在爪端) │
└──────────────┘  MSG_TARGET（目标偏差+置信度；≥10Hz心跳） └──────────────┘
```

- **P4 = 纯传感器**：只报告"看到了什么、多确定"（目标相对机体的偏差 + 置信度），
  **永不直接指挥飞行**。没有"前进/下降/抓取"之类的指令消息，以后也不会有。
- **S3 = 决策全权**：抓取状态机、下降时机、闭爪时机、所有安全判断都在飞控侧。
  P4 的输入被当作**不可信传感器**处理（见 §6 四道安全门）——P4 侧不需要为飞行安全
  负责，只需诚实报告测量值和置信度。
- 两条消息都是**无握手、无状态的单向流**：任一侧随时重启，另一侧无感知、无恢复流程。
  P4 上电后直接开始收 STATE、发 TARGET 即可。

## 2. 唯一权威定义

协议的字节级权威定义是头文件 **`components/communication/p4link_protocol.h`**。
P4 工程直接拷贝这份文件使用（双方都是 ESP32/小端，packed 结构体直接收发）。
本文档是它的展开说明；两者冲突时**以头文件为准**并请提 issue。

修改协议的流程：改头文件 → `P4LINK_PROTO_VER` +1 → 同步本文档 → 两侧同时更新拷贝。

## 3. 物理层与接线

| 参数 | 值 |
|------|-----|
| 电气 | UART, 3.3V TTL, **必须共地** |
| 波特率 | 115200, 8N1, 无流控 |
| S3 侧引脚 | TX = **GPIO17**, RX = **GPIO18**（UART2, GPIO 矩阵映射） |
| P4 侧引脚 | P4 侧自选，交叉连接：P4.RX ← S3.GPIO17, P4.TX → S3.GPIO18 |

带宽预算：STATE 线上 24B×50Hz + TARGET 18B×30Hz ≈ **1.8 KB/s**，约占 115200 波特的
16%，余量充足，不要提高波特率换取"性能"。

> S3 侧为什么是 UART2：UART0 是控制台且 GPIO44 被光流模块 TX 永久驱动，
> UART1 RX 已被光流占用。GPIO17/18 已在飞控引脚表中预留。

## 4. 帧格式

所有多字节字段**小端**。一帧：

| 偏移 | 字节 | 内容 |
|------|------|------|
| 0 | 1 | 同步字节 0 = `0xAA` |
| 1 | 1 | 同步字节 1 = `0x55` |
| 2 | 1 | `type`（消息类型，见 §5） |
| 3 | 1 | `len`（payload 字节数，≤64） |
| 4 | len | payload（packed 结构体原样） |
| 4+len | 1 | `crc_lo` |
| 5+len | 1 | `crc_hi` |

**CRC**：CRC-16/CCITT-FALSE（poly `0x1021`，init `0xFFFF`，输入/输出均不反转，无输出
异或），覆盖 **type + len + payload**（不含两个同步字节）。标准校验值：
`crc("123456789") = 0x29B1`——P4 侧实现后先跑这个自测。

⚠️ **必须使用头文件里的 `p4link_crc16()` 参考实现**（或逐位等价实现）。不要用各自
SDK 的 ROM CRC 函数——多项式/反转约定普遍不一致，这是联调时最常见的坑。

**解析器建议**（状态机）：

```
IDLE:      逐字节找 0xAA → 下一字节是 0x55 才进入 HEADER，否则回 IDLE
HEADER:    读 type, len；len > 64 → 丢弃回 IDLE
PAYLOAD:   收满 len 字节 + 2 字节 CRC
CHECK:     CRC 对 → 交付消息；错 → 整帧丢弃
           无论对错都回 IDLE 重新扫描同步字节
```

帧短、频率低，CRC 错直接丢帧即可（下一帧 20–100ms 内就到），不做重传/转义/COBS。
payload 中出现 `0xAA 0x55` 不影响正确性——解析器只在 IDLE 状态搜索同步字节。

## 5. 消息定义

### 5.1 MSG_STATE（type=0x01，S3→P4，50Hz）

P4 做"像素偏差 → 机体系米制偏差"投影所需的全部飞控状态。payload 18 字节：

| 偏移 | 字段 | 类型 | 单位/语义 |
|------|------|------|-----------|
| 0 | `ts_ms` | u32 | S3 毫秒时钟。**恒 ≥1**（S3 侧保证），P4 原样回传用 |
| 4 | `roll_cdeg` | i16 | 横滚角，0.01°（centi-degree），右倾为正 |
| 6 | `pitch_cdeg` | i16 | 俯仰角，0.01°，机头上仰为正 |
| 8 | `yaw_cdeg` | i16 | 偏航角，0.01°（参考用；无磁力计，会缓慢漂移） |
| 10 | `height_mm` | u16 | TOF 距**正下方表面**距离；**0 = 无效** |
| 12 | `vz_mms` | i16 | 垂直速度 mm/s，向上为正 |
| 14 | `flow_qual` | u8 | 光流质量原始值（参考） |
| 15 | `mode` | u8 | 0=DISARMED 1=STABILIZE 2=ALT_HOLD 3=POS_HOLD |
| 16 | `grip_deg` | u8 | 机械爪当前角 0–117（0=完全张开，110=抓取闭合预设，117=行程极限；2026-07-06 标定值） |
| 17 | `flags` | u8 | 见下 |

`flags` 位定义：

| 位 | 名称 | 含义 |
|----|------|------|
| 0 | `ARMED` | mode != DISARMED |
| 1 | `FLOW_TRUST` | 光流融合可信（quality_gain ≥ 0.05） |
| 2 | `POS_HOLD` | 位置锁激活 = 飞机在稳定悬停，**适合做视觉测量的窗口** |
| 3 | `TOF_VALID` | `height_mm` 有效 |

P4 使用要点：
- 投影解算用 `roll/pitch` + `height_mm`。`height_mm` 是到正下方表面的距离——飞到目标
  正上方后它读的是**目标顶面**，这正是"距目标平面高度"，直接可用。
- `POS_HOLD` 标志是最好的"测量窗口"信号：置位时飞机近悬停、倾角小、S3 也更愿意采信。
- 相机曝光时刻与 STATE 帧的配对由 P4 负责：用**曝光时刻最近收到的一帧** STATE 即可
  （50Hz 间隔 20ms，悬停时姿态变化小）。

### 5.2 MSG_TARGET（type=0x10，P4→S3）

**频率要求**：锁定目标时 20–30Hz；**未锁定/丢失时也必须 ≥10Hz** 发 SEARCHING/LOST 帧
——这是链路心跳，S3 超过 500ms 收不到任何有效帧就会中止抓取任务。payload 12 字节：

| 偏移 | 字段 | 类型 | 单位/语义 |
|------|------|------|-----------|
| 0 | `ts_echo_ms` | u32 | 解算所用 STATE 帧的 `ts_ms` **原样回传**；尚未收到任何 STATE 时填 **0**（S3 将该帧仅当心跳，不采信测量） |
| 4 | `dx_mm` | i16 | 目标相对机体偏差，**机体前方为正** |
| 6 | `dy_mm` | i16 | 目标相对机体偏差，**机体右方为正** |
| 8 | `conf` | u8 | 置信度 0–100（**诚实填写**，S3 丢弃 <50 的测量） |
| 9 | `track` | u8 | 0=SEARCHING（心跳，dx/dy/conf 无意义）1=LOCKED 2=LOST（曾锁定刚丢失） |
| 10 | `reserved` | u16 | v1 填 0（目标类别/ID 预留） |

**符号约定 = 飞控 `move_to` 约定**：dx>0 = 目标在机头前方（飞机需前飞），dy>0 = 目标
在机体右侧。**机头方向以飞控为准**（Web 前端"↑前"按钮方向）。

⚠️ **图像轴 → 机体轴的映射必须在集成时台架实测**，不能想当然：相机装在爪末端朝下，
安装旋转角决定映射关系。本机光流模块就曾因为旋转 90° 安装让"同一动作随机幅度"查了
一整天。验证方法（飞机通电不解锁、手持即可）：目标放在**机头前方** → `dx > 0`；放在
**机体右侧** → `dy > 0`。两个都对才算过。

**`ts_echo` 规则**：回传的是"解算这帧 TARGET 时**实际使用**的那帧 STATE"的 `ts_ms`，
不是最新收到的。S3 用 `now − ts_echo` 得到测量总陈旧度（含 P4 处理耗时 + 链路时延），
两侧时钟因此**无需同步**。P4 处理越慢，测量越容易被 S3 以"陈旧"为由丢弃（>200ms）——
这是故意的：慢测量本来就不该用于修正。

## 6. S3 侧行为承诺（P4 可以依赖的事实）

收到 LOCKED 测量后，S3 依次过四道门，全过才采信：

| # | 门 | 默认值 | 不过的后果 |
|---|-----|--------|-----------|
| 1 | 限幅 | \|dx\|,\|dy\| ≤ 1000mm | 钳位到边界 |
| 2 | 置信度 | conf ≥ 50 | 丢弃该测量 |
| 3 | 陈旧度 | now − ts_echo ≤ 200ms | 丢弃该测量 |
| 4 | 链路超时 | 任何有效帧间隔 ≤ 500ms | 超时 → 中止抓取序列，回位置保持悬停 |

外加**准静态门**：飞控倾角 \|roll\| 或 \|pitch\| > 5° 期间不采信测量（v1 不做严格
时戳补偿，只在近悬停时接受视觉修正）。

另外两条 P4 可以依赖的承诺：
- **链路超时绝不 DISARM**——p4link 不是遥控链路，P4 崩溃/重启不影响飞行安全，
  飞机会悬停在原地等 P4 回来。
- 采信的偏差走飞控现有 `move_to` 位置环（一次性移动 → 到点自动转位置保持），S3 会
  以"分段修正"节奏消费：修正 → 等稳 → 接受下一次测量。P4 不需要做任何速率匹配，
  持续发就行。

## 7. P4 侧最小实现清单

1. UART 收发（115200-8N1），拷贝 `p4link_protocol.h`
2. 按 §4 状态机解析 STATE 帧，维护"最新状态 + 本地收到时刻"
3. CRC 自测通过（`"123456789"` → `0x29B1`）
4. 相机帧 → 检测 → 像素偏差 →（用配对 STATE 的 roll/pitch/height_mm + 相机内外参）
   → 机体系 `dx_mm/dy_mm`
5. 按频率要求发 TARGET（含 SEARCHING 心跳），`ts_echo` 按 §5.2 规则填
6. 台架轴向验证（§5.2 的前/右测试）

## 8. 联调清单（P4 就绪后，按顺序）

1. **CRC 互通**：两侧各发已知帧，比对字节流与校验值
2. **丢帧率/重同步**：故意注入坏字节，确认解析器恢复、统计 CRC 错误率
3. **陈旧度标定**：实测 `now − ts_echo` 的典型值/分布（P4 处理耗时到底多大，
   200ms 门限是否合适）
4. **准静态门调参**：5° 倾角门实飞验证是否过严/过松
5. **投影精度端到端**：已知偏移的地面目标（如正前方 30cm）vs P4 报的 dx/dy，
   误差应 < ±5cm @ 悬停高度 0.5–1m

## 9. 版本与扩展

- 当前 `P4LINK_PROTO_VER = 1`。类型空间已分区：`0x0x` = S3→P4，`0x1x` = P4→S3，
  双向各有 15 个空位。
- `MSG_TARGET.reserved` 预留给目标类别/ID（多目标场景）。
- 可预见的 v2 候选（现在**不要**实现）：P4 抓取确认事件（视觉确认爪中有物）、
  多目标列表、相机曝光时刻的亚帧姿态插值。
